import React from 'react'
import ReactDOM from 'react-dom/client'
import WaybackMachine from '../index.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WaybackMachine />
  </React.StrictMode>,
)
